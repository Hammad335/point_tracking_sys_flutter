import 'package:flutter/material.dart';

class SelectPointPage extends StatelessWidget {
  const SelectPointPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
